<%= form_for(@adm_galeria) do |f| %>
    <% if @adm_galeria.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@adm_galeria.errors.count, "error") %> prohibited this adm_galeria from being saved:</h2>

          <ul>
            <% @adm_galeria.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :titulo, class: "form_label" %>
      <%= f.text_field :titulo, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :descricao, class: "form_label" %>
      <%= f.text_area :descricao, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :imagem, class: "form_label" %>
      <%= f.file_field :imagem, multiple: true %>
    </div>
    <div class="form-group">
      <%= f.submit 'Cadastrar', class: "btn btn-primary" %>
    </div>

<% end %>
