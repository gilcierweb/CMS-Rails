<%= form_for(@adm_video) do |f| %>
    <% if @adm_video.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@adm_video.errors.count, "error") %> prohibited this adm_video from being saved:</h2>

          <ul>
            <% @adm_video.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :titulo, class: "form_label" %>
      <%= f.text_field :titulo, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :url_codigo, class: "form_label" %>
      <%= f.text_field :url_codigo, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.submit 'Cadastrar', class: "btn btn-primary" %>
    </div>

<% end %>
